class Solution {
    public int arrangeCoins(int n) {
        
        //total coins is in the form of sum of first n integers
        
        long m = n;
        int row = (int)(-1 + Math.sqrt(1 + 8 *m))/ 2;
        
        return (row);
    }
}